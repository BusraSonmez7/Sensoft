<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sensofttakimi.sensoft">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SenSoft">
        <activity android:name=".Bildirimler.BildirimIcerigi"></activity>
        <activity android:name=".Bildirimler.BildirimEklemeActivity" />
        <activity android:name=".KullaniciGirisi" />
        <activity android:name=".UygulamaSayfasi" />
        <activity android:name=".SensoftHakkinda" />
        <activity android:name=".UygulamaAcilisEkrani">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>